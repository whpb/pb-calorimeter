# Code style
- You are an experienced developer with a penchant for precise, minimal code. Every line should be deliberate and justified.
- Limit functions one per file, and a maximum of 30 lines.

# File structure
- The program will be run from main.py, which should contain only logic. Functions live in functions/.

# No-gos
- Do not update README.md. If there is a discrepancy between the information provided in README.md and the functionality of the code, correct the code.

# Project origin
- This repo was forked from a sister project ("valve-tests-2") by copying its skeleton, then deleting the valve test itself. Anything that still smells of valves, cracking pressure, or solenoids is leftover, not requirement. `pyproject.toml` has since been renamed to `pb-calorimeter`. `Run.bat` hardcodes another machine's path and still says "Valve Tests" — deliberately left alone, because the final installation won't be on this PC; do not fix without being asked.
- Inherited and still correct: MODBUS read/write, settings loading, save-path resolution, sequential naming, CSV appending, console logging, connection keep-alive.

# Hardware & MODBUS notes
- The Polar Bear rig is a single Eurotherm nanodac controller, `pb1`, MODBUS TCP on port 502. `functions/connect_controllers.py` opens one persistent `ModbusTcpClient` per controller at startup and still supports several; the valve rig used two (nanodac1, nanodac2).
- Every read/write MUST pass `device_id=255` (see `UNIT_ID` in `functions/read_register.py`, reused by `write_register.py`). Without it the nanodacs accept the TCP connection but never reply — this is not a gateway setup, 255 is just the unit ID iTools itself uses. Confirmed via hours of debugging; don't reintroduce the pymodbus default of 1.
- **On the valve rig's nanodacs** (not on `pb1` — see below), registers are plain signed 16-bit integers (`DATATYPE.INT16`, `count=1`) — NOT 32-bit floats. Values arrive raw and unscaled; anything with a decimal place is a fixed-point integer that must be divided by a per-channel scale factor. INT16 wraps negative above 32767, so a channel's scale must leave headroom for its full range.
- Register entries in `settings.json` are lists whose first two elements are always `[controller, address]`; `read_register`/`write_register` slice `[:2]` off the front, so extra trailing metadata (scale, unit) can be carried per channel without touching them.
- **The Polar Bear is not a nanodac.** It is similar equipment but configured very differently, so nothing learned about register blocks or datatypes on the valve rig's nanodacs carries over to `pb1`. Do not infer `pb1`'s datatypes from the `33xxx`/`43874` address ranges.
- `PlateTemp` (`pb1/33280`) is read as **FLOAT32** — `read_register(..., float=True)`, which already reads `count=2` and converts. Flagged by the user as assumed, not confirmed. `HeaterUtil` (`pb1/43874`) is **also read as FLOAT32**, and is configured to output 0-100 as a straight percentage of `HeaterPower`. Its datatype was confirmed against the live rig on 2026-08-27, correcting an earlier INT16 guess — the first datatype on `pb1` established by observation rather than inference, so do not weaken it back to an assumption. `MasterTemp` (`pb1/33296`, the sample probe) is read as FLOAT32 too — assumed from its neighbours, not observed. No `pb1` channel needs a scale factor, and all three span two registers; the INT16 note above applies to the valve rig's nanodacs, not to `pb1`.

# Program behaviour
- `main.py` polls `programmer/UserInput` every 200 ms and dispatches on its value via the `FUNCTIONS` dict. `2` terminates the program. A dispatched function returns to the poll loop when it finishes, so a session can hold several runs. Any unmapped value falls through to `keep_alive`, which reads one register per controller so idle TCP connections aren't dropped.
- Latching is achieved by the dispatched function itself looping while `UserInput` still holds its trigger value and returning once it changes — not by a flag in `main.py`. A function that returns while its trigger is still set would be re-entered immediately.
- The whole poll cycle is wrapped in a bare `except` that prints and continues, so a transient MODBUS fault never kills a running session.
- Results go to one CSV per *function call*: `main.py` calls `resolve_save_path` again for every dispatch, so a second run in the same session gets its own timestamp and cannot overwrite the first. `save_csv` appends a raw row as each sample lands, writing the header only if the file is new; once the operator has picked the zones, `rewrite_csv` replaces that same file in full (via a temp file and `os.replace`, so it is atomic) with the derived `Q_relative`, `Plate temperature change`, `Energy` and `Zone` columns added. One file per run, deliberately — a split raw/derived pair was considered and rejected as confusing. The startup call to `resolve_save_path` exists only to give `start_console_log` a folder — its filename is discarded, and one log covers the whole session.
- `resolve_save_path` will not return a path that already exists; a timestamp that collides (two runs inside one second, possible if `TIME_STEP_S` is lowered) falls back to `next_sequential_name`.
- `Elapsed (min)` is minutes since the run's first sample — per run, not per session. `record_samples` captures its own `start = time.monotonic()`; nothing is passed in from `main.py`, whose dispatch signature is `func(clients, settings, save_path)`.
- `functions/start_console_log.py` mirrors all stdout/stderr for the session to `<results folder>/logs/<timestamp>.log` by swapping `sys.stdout`/`sys.stderr` for a tee object; `functions/stop_console_log.py` restores the originals and closes the file. The log file is opened **line buffered** (`buffering=1`): under Python's default block buffering a whole session sat in memory until close, so any death that skipped `stop_console_log` — the window's X, `taskkill /F`, a power cut — left a 0-byte log. Line buffering pushes each line to the OS, which survives process-level death; only OS-level death could still lose it, and that would need an `fsync` per line. Started right after `save_path` is resolved and stopped at the very end of `main.py`, so it misses only the "Loading settings..." print at the top (before the save folder, and therefore the logs folder, is known).
- `pyproject.toml` needs `package-mode = false` (this is a script project, not a packaged library) — plain `poetry install` fails otherwise. Requires Python >=3.13.

# Calorimetry
- The one function this project exists to provide (README function `1`). It estimates the energy added or removed by a source/sink placed on the Polar Bear's plate, by conservation of energy.
- `pb_cooling_capacity.csv` is **not** a table of watts. Its columns are `Plate Temperature, C` and `Heating Output, %` — a steady-state calibration sweep taken with no load on the plate. At each row the machine is at equilibrium, so the heater output recorded there is exactly the cooling power the machine is producing at that plate temperature. Cooling power in watts is therefore recovered as `Q_cool(T) = HeaterPower * util_curve(T) / 100`, with `HeaterPower` (193 W) from `settings.json`.
- The file has a UTF-8 BOM (read with `encoding="utf-8-sig"`) and CRLF line endings; headers are quoted (`"Plate Temperature C"`, `"Heating Output %"`). Parse with the `csv` module and `newline=""`.
- Rows are ordered by heating output, not temperature, and the cold end is non-monotonic in temperature (-45.89, -46.51, -43.88, ...). Sort by temperature before interpolating.
- The measurement then samples every time step:
  - `Q_cool = HeaterPower * interp(T_plate, curve) / 100` — cooling power at the current plate temperature.
  - `Q_abs = Q_cool - HeaterPower * util_actual / 100`, i.e. `HeaterPower * (util_curve(T_plate) - util_actual) / 100`. The physical meaning: the gap between the heater output the machine *would* need to hold this plate temperature unloaded, and what it is *actually* using, is the heat the external source is supplying. Positive `Q_relative` = energy being added to the plate.
  - `Q_relative = Q_abs - Q_baseline`, where `Q_baseline` is the **mean** `Q_abs` over a baseline zone the operator draws by hand once the run has finished — there is no fixed baselining period any more. There is roughly sinusoidal noise on the power reading with a 90-120 s period, so a baseline taken over anything but whole cycles sits off-centre in that swing and biases every sample by the same amount. Letting the operator cut the zone by eye against the plotted `Q_abs` is what makes whole cycles selectable; `summarise_window` reports the zone's `sd` and `spread` so the choice can be judged rather than guessed, and `check_baseline` warns above `SPREAD_WARN_W` (5 W).
- **Why `Q_abs` is ~0 at an unloaded baseline** (this confused the author once; it is not a claim that cooling stops). Both terms of `Q_abs` are large and nonzero at baseline — holding, say, 25 C needs real heater input against real cooling. They are also *equal*, because that is what equilibrium means and exactly what the calibration sweep recorded. Their difference is what goes to zero. `Q_abs` is a net heat balance on the plate, not an absolute cooling power, despite the name: it is the rate at which the plate is gaining heat from everything that is not the machine's own heater and cooler.
- `Q_baseline` is therefore a residual, capturing how far the machine has drifted from its calibration sweep (ambient temperature, condenser fouling, refrigerant charge, interpolation error). Subtracting it cancels that offset at the operating point, so `Q_relative` measures the change caused by the source regardless of how stale the curve is. It is printed once the zones are chosen, not at run start; beyond roughly +/-10 W (about 5 percentage points of heater output) the curve probably no longer describes the machine and it wants recalibrating — an advisory, not a hard limit.
- Baseline subtraction cancels the offset **inside the baseline zone**, not drift between that zone and the experiment zone. If ambient temperature moves between them, `Q_cool` shifts and the change is misattributed to the source. Picking the two zones close together limits this; picking a baseline far from the experiment maximises it. On long runs this is the dominant error, and it integrates: a 2 W drift over 30 minutes is 3.6 kJ of phantom energy. Keep runs short, or ambient stable, or both.
  - `E_t = E_{t-1} + Q_relative * t_step`.
- All Q values are watts, energy joules, `t_step` seconds. Use the *measured* interval between samples for `t_step`, not the nominal setting — a blocked MODBUS read would otherwise silently corrupt the integral.
- **Conditioning:** above about -29 C the curve is a steady ~2.4-3.2 C per %, so a 1 C temperature error costs only ~0.7 W. Below -39 C it flattens to ~0.6 C per % and goes non-monotonic, so the same error costs ~3 W and the interpolation is ill-defined. Control temperatures should be chosen in the well-conditioned region; warn the operator if the baseline lands in the cold end.
- The curve is a *steady-state* characteristic, so instantaneous `Q_relative` is only valid at thermal equilibrium. During transients the plate's own thermal mass absorbs and releases energy and `Q` will overshoot; the energy integral is the robust number because those transient errors largely cancel once equilibrium returns. Report total energy as the headline figure.
- A run ends when `UserInput` leaves `1`. `select_windows` then opens a **blocking** Tk window: two panes on a shared time axis, `Q_abs` above for the baseline zone and the master probe below for the experiment zone, each with its own `SpanSelector`. Two selectors on two axes deliberately, so they cannot fight over mouse events. Both zones are pre-seeded to sensible defaults, so simply closing the window still yields a valid answer.
- `keep_alive` must keep running while that window is open or the rig drops the idle socket. `select_windows` pumps it from `figure.canvas.new_timer` (backend-agnostic, not raw Tk `after`) every `KEEP_ALIVE_MS`, wrapped so a MODBUS blip prints rather than killing the window.
- Matplotlib's backend is **no longer forced to Agg** — the selection window needs an interactive one, and matplotlib falls back to Agg by itself where there is no display. `savefig` works on either. `tests/conftest.py` forces Agg so the suite never opens a window.
- `MasterTemp` was configured by the author on 2026-09-01 as `["pb1", "33296"]`; it began as a `null` placeholder, and that path still works — with no address the column records blank and the lower selection pane falls back to plate temperature, labelled as unconfigured. Its FLOAT32 datatype is assumed from its neighbours and has never been observed, so check the first probe reading looks like a temperature.
- The report indicates the chosen zones: `plot_work_curve` shades both and names them in the legend, and the Typst summary carries a `baseline` and an `experiment` dict (`start_min`, `end_min`, `duration_min`, `samples`, `mean`, `sd`, `spread`). The pre-existing top-level fields (`samples`, `duration_min`, `baseline_w`, `peak_w`, `energy_j`, `direction`, `plot`, `finished`) are unchanged, so the author's template keeps rendering; `duration_min` still means the whole recording, with the experiment's own duration under `experiment.duration_min`.
- Hardware is a **single** controller, `pb1`, with `PlateTemp`, `HeaterUtil` and `MasterTemp` alongside `UserInput` under `programmer`. `HeaterUtil` is configured to read 0-100 as a straight percentage. All three are read as FLOAT32.

# Current status
- `measure_calorimetry` is implemented and wired to `FUNCTIONS = {1: ...}` in `main.py`. Its chain is `load_cooling_curve` -> `record_samples` (looping `calculate_heat_flow`, itself calling `interpolate_cooling_power`) -> `select_windows` (via `plot_selection_panes`) -> `summarise_window` -> `check_baseline` -> `integrate_energy` -> `rewrite_csv` -> `generate_report` -> `plot_work_curve` and `compile_report`.
- Verified offline only, against simulated data: a 12 min run carrying a +/-3 W 105 s swing on top of 7.7 W of drift, with a 20 W source switched on at 5 min. A baseline zone spanning two whole cycles recovered the drift as exactly 7.7 W, and the integral came to 7780 J against a true 7800 J — the shortfall is the zone's opening sample contributing a zero-width step, not an error. Interpolation round-trips exactly at every curve knot. The measurement has **never yet been run end to end against real hardware**, though `HeaterUtil`'s datatype is now confirmed there. `PlateTemp` and `MasterTemp` as FLOAT32 remain assumed; a wrong datatype returns obvious garbage rather than a plausible wrong number, so sanity-check the traces in the selection window before drawing any zones.
- `TIME_STEP_S` (in `record_samples.py`) and `KEEP_ALIVE_MS` (in `select_windows.py`) are module constants, deliberately not `settings.json` entries: the README's settings table lists only `SavePath` and `FileName`, so adding one would create a README discrepancy. `measure_baseline.py` and its `BASELINE_PERIOD_S` are gone — the operator's selection replaced them.
- README was brought in line with operator-selected zones by the author on 2026-09-01 and now matches the code; no known discrepancies stand.
- A sample that raises is skipped with a printed warning and the integral carries straight through it (zero-order hold over the longer gap), so one transient MODBUS fault cannot cost a long run its report.
- Reporting is Typst. `generate_report` builds a summary dict and hands it to `compile_report`, which copies `report_template.typ` from the repo root into the results folder alongside a `<stem>.json` of the summary, then compiles with `root` set to that folder — so the template reaches its data and the plot PNG by bare filename. The template is the author's, and now carries a logo, a summary table, the work-curve plot and a baseline-zone table; it reads `json(sys.inputs.data)`. `compile_report` also copies the repo's `assets/` folder into the results folder — the Typst root is that folder, so repo-side images would otherwise be unreachable, and anything the template loads from disk must live in `assets/`. Typst is the `typst` PyPI package (bundled compiler), not a CLI on PATH, because the rig PC won't have one installed. A render failure prints a warning and leaves the `.typ` and `.json` behind rather than raising, so a long run never loses its numbers to a template typo.
- Tests live in `tests/`, one file per function, run with `pytest`. `tests/conftest.py` holds a `FakeClient` that encodes canned values through the real pymodbus converters, so datatype handling is genuinely exercised. Nothing in the suite opens a socket or a window; `select_windows` is tested by driving its callbacks directly.
- `write_register.py` is now unused — nothing in the calorimetry path writes to the rig. Left in place as inherited infrastructure.
